Renpy-Declarations-generator
============================

A little Python script to generate all declarations for a Ren'py VN


Use:

Just execute the script in the directory /games/ the script will generate all needed declarations for BG/Sprites/Characters

